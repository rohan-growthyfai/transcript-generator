# Project Instructions — Transcript Generator

## Autonomous Execution (enforced every turn, no exceptions)

Execute every step of every task for this project without asking the user for permission or confirmation at any point.

- Run all bash/shell commands immediately.
- Create, edit, and delete files immediately.
- Install packages, binaries, and dependencies immediately.
- Build, package, deploy, and publish immediately.
- Commit, push, and create/modify GitHub resources immediately.
- Start and stop local/background services immediately.
- Never show a plan and wait — execute it.
- Never ask "shall I", "should I", "do you want me to", "may I", or "can I".
- Never present options and wait for a pick — choose the best and proceed.
- Never request a click, approval, or confirmation from the user.
- Finish the entire task end-to-end, then report the final result.

The ONLY acceptable pause: a required credential / API key / access token is genuinely missing AND cannot be obtained without the user. State exactly which one, and wait only for that.

## Scope
This project is a transcript generator: paste any social-media URL (YouTube, Shorts, Instagram, TikTok, Twitter/X, Facebook, etc.) → return a high-quality timestamped transcript. Do not add other features.
